apiVersion: v1
kind: Service
metadata:
  name: {{ include "pushpaka.fullname" . }}-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pushpaka.labels" . | nindent 4 }}
    component: dashboard
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
    - port: {{ .Values.dashboard.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pushpaka.selectorLabels" . | nindent 4 }}
    component: dashboard
